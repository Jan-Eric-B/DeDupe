<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="DeDupe.Views.Pages.FileInputPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:converters="using:DeDupe.Convertes"
    xmlns:models="using:DeDupe.Models"
    xmlns:pages="using:DeDupe.ViewModels.Pages"
    d:DataContext="{d:DesignInstance Type=pages:FileInputViewModel}"
    mc:Ignorable="d">
    <Page.Resources>
        <converters:BoolToColorConverter x:Key="BoolToColorConverter" />
        <converters:InverseBoolConverter x:Key="InverseBoolConverter" />
    </Page.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Title -->
        <TextBlock Grid.Row="0"
           Text="{x:Bind ViewModel.Title, Mode=OneWay}"
           Style="{StaticResource TitleTextBlockStyle}" />

        <!-- Content -->
        <Grid Grid.Row="1" ColumnSpacing="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="3*" />
                <ColumnDefinition Width="7*" />
            </Grid.ColumnDefinitions>

            <!-- Options -->
            <StackPanel VerticalAlignment="Bottom" Grid.Column="0" Spacing="10" Margin="0,20,0,42">

                <!-- Include Video Files -->
                <CheckBox IsChecked="{x:Bind ViewModel.IncludeVideoFiles, Mode=TwoWay}" IsEnabled="False">WIP - Include Video / Animated files</CheckBox>

                <Grid
            Padding="10"
            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
            BorderThickness="1"
            CornerRadius="8"
            Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="auto" />
                        <RowDefinition Height="auto" />
                        <RowDefinition Height="auto" />
                        <RowDefinition Height="auto" />
                        <RowDefinition Height="auto" />
                        <RowDefinition Height="auto" />
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Grid.ColumnSpan="2">Frame Extraction</TextBlock>

                    <RadioButton GroupName="FrameExtraction" Grid.Row="1" IsEnabled="{x:Bind ViewModel.IncludeVideoFiles, Mode=OneWay}" IsChecked="True">Time-Based Interval</RadioButton>
                    <RadioButton GroupName="FrameExtraction" Grid.Row="2" IsEnabled="{x:Bind ViewModel.IncludeVideoFiles, Mode=OneWay}">Fixed Interval</RadioButton>
                    <RadioButton GroupName="FrameExtraction" Grid.Row="3" IsEnabled="{x:Bind ViewModel.IncludeVideoFiles, Mode=OneWay}">Chapter Based</RadioButton>
                    <RadioButton GroupName="FrameExtraction" Grid.Row="4" IsEnabled="{x:Bind ViewModel.IncludeVideoFiles, Mode=OneWay}">Single Frame</RadioButton>

                    <StackPanel Orientation="Horizontal" Grid.Column="1" Grid.Row="1" Spacing="10">
                        <NumberBox Height="25" MinHeight="0" Padding="0" IsEnabled="{x:Bind ViewModel.IncludeVideoFiles, Mode=OneWay}"></NumberBox>
                        <TextBlock VerticalAlignment="Center" Foreground="{x:Bind ViewModel.IncludeVideoFiles, Mode=OneWay, Converter={StaticResource BoolToColorConverter}}">Frames each</TextBlock>
                        <DropDownButton IsEnabled="{x:Bind ViewModel.IncludeVideoFiles, Mode=OneWay}">Min</DropDownButton>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" Grid.Column="1" Grid.Row="2" Spacing="10">
                        <TextBlock VerticalAlignment="Center" Foreground="{x:Bind ViewModel.IncludeVideoFiles, Mode=OneWay, Converter={StaticResource BoolToColorConverter}}">Every</TextBlock>
                        <NumberBox Height="25" MinHeight="0" Padding="0" IsEnabled="{x:Bind ViewModel.IncludeVideoFiles, Mode=OneWay}"></NumberBox>
                        <TextBlock VerticalAlignment="Center" Foreground="{x:Bind ViewModel.IncludeVideoFiles, Mode=OneWay, Converter={StaticResource BoolToColorConverter}}">th Frame</TextBlock>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" Grid.Column="1" Grid.Row="3" Spacing="10">
                        <TextBlock VerticalAlignment="Center" Foreground="{x:Bind ViewModel.IncludeVideoFiles, Mode=OneWay, Converter={StaticResource BoolToColorConverter}}">Offset</TextBlock>
                        <NumberBox Height="25" MinHeight="0" Padding="0" IsEnabled="{x:Bind ViewModel.IncludeVideoFiles, Mode=OneWay}"></NumberBox>
                        <DropDownButton IsEnabled="{x:Bind ViewModel.IncludeVideoFiles, Mode=OneWay}">Min</DropDownButton>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" Grid.Column="1" Grid.Row="4" Spacing="10">
                        <TextBlock VerticalAlignment="Center" Foreground="{x:Bind ViewModel.IncludeVideoFiles, Mode=OneWay, Converter={StaticResource BoolToColorConverter}}">Timestamp</TextBlock>
                        <TextBox Height="25" MinHeight="0" Padding="0" IsEnabled="{x:Bind ViewModel.IncludeVideoFiles, Mode=OneWay}"></TextBox>
                    </StackPanel>

                    <CheckBox Grid.Row="5" Grid.ColumnSpan="2" IsEnabled="{x:Bind ViewModel.IncludeVideoFiles, Mode=OneWay}">Use all Frames from Gif / WEBP</CheckBox>
                </Grid>
            </StackPanel>

            <!-- Item List -->
            <Grid Grid.Column="1" RowSpacing="10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <ListView x:Name="lvMediaSelectionList"
            Grid.Row="0"
            ItemsSource="{x:Bind ViewModel.MediaPathItems, Mode=OneWay}"
            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
            BorderThickness="1"
            Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
            CornerRadius="8"
            SelectionMode="None"
            AllowDrop="True"
            DragOver="lvMediaSelectionList_DragOver"
            Drop="lvMediaSelectionList_Drop">
                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="models:MediaPathItem">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>

                                <!-- Item Icon -->
                                <FontIcon Grid.Column="0" Margin="0,0,10,0" Glyph="{x:Bind IconGlyph, Mode=OneWay}" VerticalAlignment="Center" />

                                <!-- Item Path -->
                                <TextBlock Grid.Column="1" Text="{x:Bind Path}" ToolTipService.ToolTip="{x:Bind Path}" VerticalAlignment="Center" TextWrapping="NoWrap" TextTrimming="CharacterEllipsis" />

                                <!-- Include Subdirectories Checkbox - Only visible for folders with subdirectories -->
                                <CheckBox Grid.Column="2" Margin="10,0" IsChecked="{x:Bind IncludeSubdirectories, Mode=TwoWay}" Content="Include Subdirectories" VerticalAlignment="Center" Visibility="{x:Bind ShowSubdirectoriesCheckbox}" />
                                <!-- Remove Button -->

                                <Button Grid.Column="3" Command="{Binding ElementName=lvMediaSelectionList, Path=DataContext.RemoveItemCommand}" CommandParameter="{Binding}" VerticalAlignment="Center" Background="Transparent" BorderThickness="0" IsEnabled="{Binding ElementName=lvMediaSelectionList, Path=DataContext.IsBusy, Converter={StaticResource InverseBoolConverter}}">
                                    <FontIcon Glyph="&#xE74D;" />
                                </Button>
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>

                    <!-- Empty state message -->
                    <ListView.ItemsPanel>
                        <ItemsPanelTemplate>
                            <ItemsStackPanel />
                        </ItemsPanelTemplate>
                    </ListView.ItemsPanel>
                </ListView>

                <StackPanel Orientation="Horizontal" Grid.Row="1" HorizontalAlignment="Left" Spacing="10">
                    <Button x:Name="SelectFolderButton" Content="Select Folder" Command="{x:Bind ViewModel.SelectFolderCommand}" IsEnabled="{Binding IsBusy, Converter={StaticResource InverseBoolConverter}}" />
                    <Button x:Name="SelectFilesButton" Content="Select Files" Command="{x:Bind ViewModel.SelectFilesCommand}" IsEnabled="{Binding IsBusy, Converter={StaticResource InverseBoolConverter}}" />
                    <TextBlock VerticalAlignment="Center" Foreground="{ThemeResource TextFillColorSecondaryBrush}" Text="{x:Bind ViewModel.FileCountText, Mode=OneWay}" />
                </StackPanel>
            </Grid>
        </Grid>

        <!-- Navigation Buttons -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Button Content="Previous"
        Grid.Column="0"
        HorizontalAlignment="Left"
        Command="{x:Bind ViewModel.NavigateToPreviousCommand}" />

            <StackPanel Orientation="Horizontal"
            Grid.Column="1"
            Spacing="10"
            HorizontalAlignment="Right">
                <Button Content="Complete"
        Command="{x:Bind ViewModel.CompleteCommand}" />

                <Button Content="Next"
        Command="{x:Bind ViewModel.NavigateToNextCommand}"
        IsEnabled="{x:Bind ViewModel.CanNavigateToNext, Mode=OneWay}"
        Style="{StaticResource AccentButtonStyle}" />
            </StackPanel>
        </Grid>
    </Grid>
</Page>