<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="DeDupe.Views.Settings.ModelSettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:constants="using:DeDupe.Constants"
    xmlns:controls="using:CommunityToolkit.WinUI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="using:CommunityToolkit.WinUI"
    Background="Transparent"
    mc:Ignorable="d">
    <Page.Resources>
        <ResourceDictionary>
            <ResourceDictionary.ThemeDictionaries>
                <ResourceDictionary x:Key="Light">
                    <SvgImageSource x:Key="GitHubIcon" UriSource="ms-appx:///Assets/Images/GitHub_Invertocat_Black_Clearspace.svg" />
                </ResourceDictionary>
                <ResourceDictionary x:Key="Dark">
                    <SvgImageSource x:Key="GitHubIcon" UriSource="ms-appx:///Assets/Images/GitHub_Invertocat_White_Clearspace.svg" />
                </ResourceDictionary>
            </ResourceDictionary.ThemeDictionaries>
            <SvgImageSource x:Key="HuggingFaceIcon" UriSource="ms-appx:///Assets/Images/huggingface_logo-noborder.svg" />
        </ResourceDictionary>
    </Page.Resources>
    <ScrollViewer>
        <StackPanel Spacing="4">
            <TextBlock
                Margin="0,0,0,8"
                Style="{StaticResource TitleTextBlockStyle}"
                Text="Model configuration" />

            <!--  Model  -->
            <TextBlock
                Margin="0,0,0,3"
                Style="{StaticResource BodyStrongTextBlockStyle}"
                Text="Model selection" />

            <!--  Model Source Toggle  -->
            <controls:SettingsCard
                Description="Switch between the bundled model or your own ONNX file."
                Header="Model source"
                HeaderIcon="{ui:FontIcon Glyph=&#xE8F1;}">
                <controls:Segmented x:Name="ModelSourceSegmented" SelectionChanged="ModelSourceSegmented_SelectionChanged">
                    <controls:SegmentedItem Content="Bundled" Tag="Bundled" />
                    <controls:SegmentedItem Content="Custom" Tag="Custom" />
                </controls:Segmented>
            </controls:SettingsCard>

            <!--  Bundled Model Info  -->
            <StackPanel
                x:Name="BundledModelSection"
                Margin="0,8,0,0"
                Spacing="8"
                Visibility="{x:Bind ViewModel.UseBundledModel, Mode=OneWay}">

                <controls:SettingsCard
                    Description="{x:Bind ViewModel.BundledModelDevelopers}"
                    Header="{x:Bind ViewModel.BundledModelDisplayName}"
                    HeaderIcon="{ui:FontIcon Glyph=&#xE74C;}">
                </controls:SettingsCard>
            </StackPanel>

            <!--  Custom Model Selection  -->
            <StackPanel
                x:Name="CustomModelSection"
                Margin="0,8,0,0"
                Spacing="8"
                Visibility="{x:Bind ViewModel.UseCustomModel, Mode=OneWay}">

                <!--  File picker  -->
                <controls:SettingsCard
                    Description="Drag and drop, or click Browse to select your .onnx file"
                    Header="Model file"
                    HeaderIcon="{ui:FontIcon Glyph=&#xF003;}">
                    <Grid
                        x:Name="ModelFileDropGrid"
                        AllowDrop="True"
                        ColumnSpacing="8"
                        DragOver="ModelFileDropGrid_DragOver"
                        Drop="ModelFileDropGrid_Drop">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <Border
                            Grid.Column="0"
                            MinWidth="280"
                            MinHeight="36"
                            Padding="12,0"
                            VerticalAlignment="Center"
                            Background="{ThemeResource ControlFillColorDefaultBrush}"
                            BorderBrush="{ThemeResource ControlStrokeColorDefaultBrush}"
                            BorderThickness="1"
                            CornerRadius="{ThemeResource ControlCornerRadius}"
                            DoubleTapped="ModelFileTextBlock_DoubleTapped"
                            ToolTipService.ToolTip="{x:Bind ViewModel.CustomModelFilePath, Mode=OneWay}">
                            <TextBlock
                                VerticalAlignment="Center"
                                Text="{x:Bind ViewModel.CustomFileName, Mode=OneWay}"
                                TextTrimming="CharacterEllipsis">
                                <TextBlock.Foreground>
                                    <SolidColorBrush Color="{ThemeResource TextFillColorSecondary}" />
                                </TextBlock.Foreground>
                            </TextBlock>
                        </Border>

                        <Button
                            Grid.Column="1"
                            Command="{x:Bind ViewModel.SelectCustomModelFileCommand}"
                            Content="Browse"
                            Style="{StaticResource AccentButtonStyle}" />
                    </Grid>
                </controls:SettingsCard>

                <!--  Download links  -->
                <controls:SettingsCard
                    Description="Find compatible ONNX models from these repositories"
                    Header="Need a model?"
                    HeaderIcon="{ui:FontIcon Glyph=&#xE896;}">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <HyperlinkButton
                            Command="{x:Bind ViewModel.OpenLinkCommand}"
                            CommandParameter="{x:Bind constants:ModelLinks.HuggingFace}"
                            ToolTipService.ToolTip="{x:Bind constants:ModelLinks.HuggingFace}">
                            <StackPanel Orientation="Horizontal" Spacing="5">
                                <Image
                                    Width="25"
                                    Height="25"
                                    VerticalAlignment="Center"
                                    Source="{StaticResource HuggingFaceIcon}" />
                                <TextBlock VerticalAlignment="Center" Text="Hugging Face" />
                            </StackPanel>
                        </HyperlinkButton>
                        <HyperlinkButton
                            Command="{x:Bind ViewModel.OpenLinkCommand}"
                            CommandParameter="{x:Bind constants:ModelLinks.OnnxModelZoo}"
                            ToolTipService.ToolTip="{x:Bind constants:ModelLinks.OnnxModelZoo}">
                            <StackPanel Orientation="Horizontal" Spacing="5">
                                <Image
                                    Width="25"
                                    Height="25"
                                    VerticalAlignment="Center"
                                    Source="{ThemeResource GitHubIcon}" />
                                <TextBlock VerticalAlignment="Center" Text="ONNX Model Zoo" />
                            </StackPanel>
                        </HyperlinkButton>
                    </StackPanel>
                </controls:SettingsCard>
            </StackPanel>

            <!--  Advanced  -->
            <TextBlock
                Margin="0,29,0,3"
                Style="{StaticResource BodyStrongTextBlockStyle}"
                Text="Advanced" />

            <controls:SettingsExpander
                Description="Configure image preprocessing. Auto-applied when switching back to the bundled model."
                Header="Normalization"
                HeaderIcon="{ui:FontIcon Glyph=&#xE9D9;}"
                IsExpanded="False">

                <StackPanel Orientation="Horizontal" Spacing="8">
                    <Button
                        Command="{x:Bind ViewModel.ApplyBundledModelNormalizationCommand}"
                        Content="Apply bundled model defaults"
                        IsEnabled="{x:Bind ViewModel.UseBundledModel, Mode=OneWay}" />
                </StackPanel>

                <controls:SettingsExpander.Items>
                    <controls:SettingsCard Header="Mean (R, G, B)">
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <NumberBox
                                Width="110"
                                BorderBrush="Red"
                                LargeChange="0.01"
                                SmallChange="0.001"
                                SpinButtonPlacementMode="Compact"
                                Value="{x:Bind ViewModel.MeanR, Mode=TwoWay}" />
                            <NumberBox
                                Width="110"
                                BorderBrush="Green"
                                LargeChange="0.01"
                                SmallChange="0.001"
                                SpinButtonPlacementMode="Compact"
                                Value="{x:Bind ViewModel.MeanG, Mode=TwoWay}" />
                            <NumberBox
                                Width="110"
                                BorderBrush="Blue"
                                LargeChange="0.01"
                                SmallChange="0.001"
                                SpinButtonPlacementMode="Compact"
                                Value="{x:Bind ViewModel.MeanB, Mode=TwoWay}" />
                        </StackPanel>
                    </controls:SettingsCard>

                    <controls:SettingsCard Header="Std Dev (R, G, B)">
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <NumberBox
                                Width="110"
                                BorderBrush="Red"
                                LargeChange="0.01"
                                SmallChange="0.001"
                                SpinButtonPlacementMode="Compact"
                                Value="{x:Bind ViewModel.StdR, Mode=TwoWay}" />
                            <NumberBox
                                Width="110"
                                BorderBrush="Green"
                                LargeChange="0.01"
                                SmallChange="0.001"
                                SpinButtonPlacementMode="Compact"
                                Value="{x:Bind ViewModel.StdG, Mode=TwoWay}" />
                            <NumberBox
                                Width="110"
                                BorderBrush="Blue"
                                LargeChange="0.01"
                                SmallChange="0.001"
                                SpinButtonPlacementMode="Compact"
                                Value="{x:Bind ViewModel.StdB, Mode=TwoWay}" />
                        </StackPanel>
                    </controls:SettingsCard>
                </controls:SettingsExpander.Items>
            </controls:SettingsExpander>
        </StackPanel>
    </ScrollViewer>
</Page>
