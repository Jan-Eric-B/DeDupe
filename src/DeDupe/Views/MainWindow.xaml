<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="DeDupe.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:DeDupe"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="DeDupe">
    <Grid x:Name="grdRoot" Padding="5">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Title -->
        <StackPanel x:Name="spTitle" Orientation="Horizontal" Grid.Row="0" Padding="0,5" Spacing="5" Background="Transparent">
            <Image Source="/Assets/StoreLogo.png" Width="16" Height="16" VerticalAlignment="Center" />
            <TextBlock Text="DeDupe" VerticalAlignment="Center" Style="{StaticResource CaptionTextBlockStyle}" Foreground="{ThemeResource TextFillColorPrimaryBrush}" />
        </StackPanel>

        <!-- Top -->
        <Grid Grid.Row="1" Background="Red">
            <Frame x:Name="frMain" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" />
        </Grid>

        <!-- Bottom -->
        <Grid Grid.Row="2" Background="Green">
        </Grid>
    </Grid>
</Window>