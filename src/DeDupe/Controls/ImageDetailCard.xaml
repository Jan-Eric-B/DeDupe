<?xml version="1.0" encoding="utf-8" ?>
<UserControl
    x:Class="DeDupe.Controls.ImageDetailCard"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:DeDupe.Models.Analysis"
    d:DataContext="{d:DesignInstance Type=models:SelectableItem}"
    Loaded="OnLoaded"
    Unloaded="OnUnloaded"
    mc:Ignorable="d">
    <Border
        x:Name="CardBorder"
        Width="180"
        Padding="8"
        Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}"
        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
        BorderThickness="1"
        CornerRadius="8">
        <Grid RowSpacing="6">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!--  Item  -->
            <Grid Grid.Row="0">
                <Border
                    Height="120"
                    Background="{ThemeResource LayerFillColorDefaultBrush}"
                    CornerRadius="6"
                    DoubleTapped="OpenItem_DoubleTapped"
                    ToolTipService.ToolTip="Double-click to open">
                    <Grid>
                        <!--  Placeholder  -->
                        <FontIcon
                            x:Name="PlaceholderIcon"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            FontSize="48"
                            Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                            Glyph="&#xEB9F;" />

                        <!--  Thumbnail  -->
                        <Image
                            x:Name="ImageThumbnail"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Stretch="UniformToFill" />
                    </Grid>
                </Border>

                <!--  Selection Checkbox  -->
                <CheckBox
                    x:Name="SelectionCheckBox"
                    MinWidth="0"
                    MinHeight="0"
                    Margin="4"
                    Padding="0"
                    HorizontalAlignment="Left"
                    VerticalAlignment="Top"
                    Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    CornerRadius="4" />

                <!--  Image Format Badge  -->
                <Border
                    Margin="4"
                    Padding="6,2"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Top"
                    Background="{ThemeResource AcrylicBackgroundFillColorBaseBrush}"
                    CornerRadius="4">
                    <TextBlock
                        x:Name="FormatTextBlock"
                        FontSize="10"
                        FontWeight="SemiBold"
                        Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                        Text="PNG" />
                </Border>
            </Grid>

            <!--  File Name  -->
            <TextBlock
                x:Name="FileNameTextBlock"
                Grid.Row="1"
                DoubleTapped="FileName_DoubleTapped"
                FontSize="12"
                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                Text="filename.jpg (Placeholder)"
                TextTrimming="CharacterEllipsis"
                TextWrapping="NoWrap">
                <ToolTipService.ToolTip>
                    <ToolTip>
                        <StackPanel Spacing="4">
                            <TextBlock Text="{Binding SelectableItem.FilePath, RelativeSource={RelativeSource Mode=TemplatedParent}}" />
                            <TextBlock
                                FontSize="11"
                                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                Text="Double-click to open folder" />
                        </StackPanel>
                    </ToolTip>
                </ToolTipService.ToolTip>
            </TextBlock>

            <!--  Metadata Values  -->
            <Grid Grid.Row="2" RowSpacing="3">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <!--  Resolution  -->
                <Grid Grid.Row="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <FontIcon
                        Grid.Column="0"
                        Margin="0,0,6,0"
                        VerticalAlignment="Center"
                        FontSize="12"
                        Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                        Glyph="&#xE799;" />
                    <TextBlock
                        x:Name="ResolutionTextBlock"
                        Grid.Column="1"
                        VerticalAlignment="Center"
                        FontSize="13"
                        FontWeight="SemiBold"
                        Text="1920×1080 (Placeholder)" />
                </Grid>

                <!--  File Size  -->
                <Grid Grid.Row="1">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <FontIcon
                        Grid.Column="0"
                        Margin="0,0,6,0"
                        VerticalAlignment="Center"
                        FontSize="12"
                        Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                        Glyph="&#xE8B7;" />
                    <TextBlock
                        x:Name="FileSizeTextBlock"
                        Grid.Column="1"
                        VerticalAlignment="Center"
                        FontSize="13"
                        FontWeight="SemiBold"
                        Text="2.5 MB (Placeholder)" />
                </Grid>

                <!--  Creation Date  -->
                <Grid Grid.Row="2">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <FontIcon
                        Grid.Column="0"
                        Margin="0,0,6,0"
                        VerticalAlignment="Center"
                        FontSize="12"
                        Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                        Glyph="&#xE787;" />
                    <TextBlock
                        x:Name="CreatedDateTextBlock"
                        Grid.Column="1"
                        VerticalAlignment="Center"
                        FontSize="12"
                        Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                        Text="13.12.2025 (Placeholder)" />
                </Grid>
            </Grid>
        </Grid>

        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup x:Name="SelectionStates">
                <VisualState x:Name="Unselected" />
                <VisualState x:Name="Selected">
                    <VisualState.Setters>
                        <Setter Target="CardBorder.BorderBrush" Value="{ThemeResource AccentFillColorSecondaryBrush}" />
                        <Setter Target="CardBorder.BorderThickness" Value="2" />
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>
    </Border>
</UserControl>